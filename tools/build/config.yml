domain: "timbur.tech"

paths: # relative
  templates: "_templates"
  pages: "_pages"
  styles: "_assets/styles"
  media: "_assets/media"
  build: "_site"

includes: # direct copy
  - _assets
  - _data

defaults:
  layout: default